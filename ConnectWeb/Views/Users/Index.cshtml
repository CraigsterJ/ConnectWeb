@model IEnumerable<ConnectWeb.Models.DataModels.User>
@{
    ViewData["Title"] = "Users for Applications";
}
<h2>Users for @ViewBag.ApplicationName</h2>

<p>
    <a asp-controller="Users" asp-action="Create" asp-route-appId="@ViewBag.AppId" >Create New</a>
</p>
<div class="row">
    <table class="table table-bordered table-striped">   
        <tr>
            <th>Id</th>
            <th>Guid</th>
            <th>User Name</th>
            <th>Full Name</th>
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FullName)
            </td>
            <td>
                <a asp-action="Edit" asp-controller="Users" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Delete" asp-controller="Users" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
        }
    </table>
</div>
<div>
    <a asp-action="Index" asp-controller="Application">Back to Applications</a>
</div>
